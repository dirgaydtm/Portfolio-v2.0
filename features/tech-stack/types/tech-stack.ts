import { type IconType } from "react-icons";

// ============ TECH STACK TYPES ============
export interface TechStack {
    name: string;
    icon: IconType;
    category: string;
    group: string;
    className?: string;
}

